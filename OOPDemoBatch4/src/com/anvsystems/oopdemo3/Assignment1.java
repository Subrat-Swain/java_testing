package com.anvsystems.oopdemo3;

public class Assignment1 {
	/**
	 * Declare a class called as MailServer
	 * Declare properties
	 * serviceProvider
	 * protocol
	 * mailCapapcity
	 * mailLatency
	 * 
	 * Set Access Priviledge as per standard
	 * Implement Getters and Setters
	 * 
	 * Declare another class called as Gmail
	 * Gmail should extend MailServer
	 * Gmail should have methods like sendMail, readMail which uses properties
	 * from mailServer
	 * Print Properties
	 */
}
